@page
@model PrivacyModel

<section class="section" style="padding-top: calc(5.25rem + var(--section-y))">
    <div class="shell shell--content">
        <p class="eyebrow">@L["footer.privacy"]</p>
        <h1 class="h-section" style="max-width: 18ch">@L["privacy.lede"]</h1>
        <hr class="rule" />

        <div class="prose" style="display: grid; gap: 2.5rem; margin-top: 3rem">
            <div>
                <h2 class="h-card" style="margin-bottom: .75rem">@L["privacy.cookies.title"]</h2>
                <p>@L["privacy.cookies.body"]</p>
            </div>
            <div>
                <h2 class="h-card" style="margin-bottom: .75rem">@L["privacy.storage.title"]</h2>
                <p>@L["privacy.storage.body"]</p>
            </div>
            <div>
                <h2 class="h-card" style="margin-bottom: .75rem">@L["privacy.tracking.title"]</h2>
                <p>@L["privacy.tracking.body"]</p>
            </div>
            <div>
                <h2 class="h-card" style="margin-bottom: .75rem">@L["privacy.contact.title"]</h2>
                <p>@L["privacy.contact.body"]</p>
            </div>
        </div>
    </div>
</section>
