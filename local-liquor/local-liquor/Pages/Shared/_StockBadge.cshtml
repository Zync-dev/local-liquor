@model WineView
@if (Model.Stock != local_liquor.Data.Entities.StockStatus.Available)
{
    <span class="stock-badge stock-badge--@(Model.IsSoldOut ? "out" : "low")">
        @(Model.IsSoldOut ? L["stock.soldout"] : L["stock.low"])
    </span>
}
